import React from 'react'

const FragmentConcept = () => {
  return (
    //Fragment allows grouping of multiple elements without extra dom nodes
    //it returns multiple elements without a wrapping paremt
    //short syntax <> </> 
  <React.Fragment>

  </React.Fragment>
)
}

export default FragmentConcept